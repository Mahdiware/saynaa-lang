v1 = 2==3 ? true : false
assert(v1 == false)

v2 = 1==1 ? "string" : Bool // Bool is class builtin
assert(v2 == "string")

v3 = true ? 2 ? "ok" ? [2, 3] ? {1:3} ? "done" : 0 : false : null : "" : {}
assert(v3 == "done")